import { ChordHighlighterPipe } from './chord-highlighter.pipe';

describe('ChordHighlighterPipe', () => {
  it('create an instance', () => {
    const pipe = new ChordHighlighterPipe();
    expect(pipe).toBeTruthy();
  });
});
